# === DATABASE ===
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/prospecting

# === APP ===
DEBUG=false

# === SMTP (pour le module Email) ===
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_NAME=Sales Prospecting Tool
SMTP_FROM_EMAIL=
